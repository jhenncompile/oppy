{
  "name": "oppy-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agente que descubre, verifica y recomienda oportunidades personalizadas",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "cron": "node src/jobs/runner.js",
    "recordatorios": "node src/jobs/recordatoriosJob.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@zavudev/sdk": "^0.51.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  }
}
